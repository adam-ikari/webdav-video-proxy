# SDD Progress Ledger
Task 1: complete (commits 5b4dc31..24e79ea, review clean) — scaffold, config, Dockerfile. Minors: TestLoadFromEnv could use t.Setenv; getenverrors silent; Dockerfile forward-looking (cmd/proxy/go.sum arrive later). All brief-mandated/out-of-scope.
Task 2: complete (commits 24e79ea..b75136f, review clean) — SubSource/ParseSubSource/Key/RestPath. Minors: RestPath assumes matching path (no validation); Key '|' separator theoretical collision. Both brief-mandated.
Task 3: complete (commits b75136f..215f022, review clean) — SQLite store (profiles/samples/blocks), modernc.org/sqlite pure-Go no CGO. Important(non-blocking): SetMaxOpenConns(1)+WAL throughput ceiling, silent last_used UPDATE error — revisit in §6. Minors: global clock race, HasBlock returns (true,err) on error, ListLRUBlocks tie-break unspecified.
Task 4: complete — upstream WebDAV client (GetRange/Head/PropFind), stdlib net/http + encoding/xml only. Brief-internal contradiction: PROPFIND test stub omitted WriteHeader(207) so implicit 200 failed client's strict 207 check; fixed test stub to send http.StatusMultiStatus (client kept verbatim, correct per RFC 4918). Minors(non-blocking): PropFind sets req.Body but no Content-Length (Go sets chunked/0; most servers tolerant); GetRange accepts 200 fallback (whole-file) which may misreport total via Content-Length; parseTotal ignores malformed Content-Range silently (returns 0).
